fun fact(n) {
  if (n <= 1) return 1;
  return n * fact(n - 1);
}
print fact(6);
fun ackermann(m, n) {
  if (m == 0) return n + 1;
  if (n == 0) return ackermann(m - 1, 1);
  return ackermann(m - 1, ackermann(m, n - 1));
}
print ackermann(2, 3);
